all:
  children:
    gpu_nodes:
      hosts:
        pc-node:
          ansible_host: # <your-pc-ip>
          ansible_user: # <your-user>
    ci_nodes:
      hosts:
        ci-pi:
          ansible_host: # <your-ci-pi-ip>
          ansible_user: # <your-user>
    rpi_nodes:
      hosts:
        # Other Raspberry Pis — NOT targeted for composer workloads
        # rpi-1:
        #   ansible_host: <ip>
