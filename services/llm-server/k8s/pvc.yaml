apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-data
  labels:
    app: llm-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # 50Gi is enough for several 7B-13B models
      storage: 50Gi
